---
author: claude
type: daily
date: 2026-07-17
---

# Session Log — Friday, July 17 2026

## What We Worked On
- Got the scheduled scraper running, then spent the session debugging why the Gemini niche step never produced output.
- Worked through a long chain of errors and finally found the root cause.
- Refined the subreddit sampling and loosened the Gemini prompt along the way.

## What Was Built or Changed
- Fixed the scheduled-run crash: `staticData.seenUrls` undefined → added a safety guard in `scoring-rss.js`.
- **Root cause of "no niches" found: 429 rate limit** — we hammered the free Gemini API with dozens of manual test runs all evening; every call was being throttled and dropped (the filter itself was fine, never got to run). Retrying the 429s made it worse.
- Fixes applied: batch `MAX_TO_CHECK` → 8; Gemini `THROTTLE_MS` → 6s; retry now excludes 429 (only 503/500). Files: `scoring-rss.js`, `monetisable-check-gemini.js` (+ DEBUG).
- Reordered Config subreddits so money/hobby subs come first (BuyItForLife, HomeImprovement, Fitness, Cooking, gardening, DIY, Frugal, YouShouldKnow, LifeProTips, AskReddit) — with a small batch, round-robin was only ever sampling the first few subs.
- Loosened the Gemini prompt to lean "yes" and use the subreddit as the niche signal (was likely too strict).
- Recurring gotcha noted: every fresh paste of the Gemini file resets the key to the `YOUR_GEMINI_KEY` placeholder — must re-insert the real key. (Rule going forward: once working, stop re-pasting; edit in place.)

## Still Open
- **Left running on the 4-hourly schedule overnight** — deliberately stopped manual testing so the rate limit resets. Each scheduled run is small + spaced, so it shouldn't 429.
- May have exhausted today's free Gemini quota — should be clear after the daily reset.
- Never yet confirmed a positive 💡 NICHE verdict end-to-end (only errors/rejections seen).

## Start Here Tomorrow
Check Telegram: did any 💡 NICHE messages land overnight once the rate limit reset? If yes → the scraper works, pick a niche and build a site. If it's still 429ing → the free per-minute limit is lower than assumed; raise the throttle more or switch to `gemini-2.0-flash-lite` (higher free limit).
